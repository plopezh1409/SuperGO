import { ServiceResponseCodes.Model } from './service-response-codes.model';

describe('ServiceResponseCodes.Model', () => {
  it('should create an instance', () => {
    expect(new ServiceResponseCodes.Model()).toBeTruthy();
  });
});
