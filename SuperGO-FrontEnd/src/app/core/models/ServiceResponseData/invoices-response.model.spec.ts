import { InvoicesResponse.Model } from './invoices-response.model';

describe('InvoicesResponse.Model', () => {
  it('should create an instance', () => {
    expect(new InvoicesResponse.Model()).toBeTruthy();
  });
});
