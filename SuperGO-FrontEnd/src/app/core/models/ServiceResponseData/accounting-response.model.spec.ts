import { AccountingResponse.Model } from './accounting-response.model';

describe('AccountingResponse.Model', () => {
  it('should create an instance', () => {
    expect(new AccountingResponse.Model()).toBeTruthy();
  });
});
