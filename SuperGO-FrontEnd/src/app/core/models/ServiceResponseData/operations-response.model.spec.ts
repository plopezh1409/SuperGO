import { OperationsResponse.Model } from './operations-response.model';

describe('OperationsResponse.Model', () => {
  it('should create an instance', () => {
    expect(new OperationsResponse.Model()).toBeTruthy();
  });
});
